<% provide(:title, 'Sign Company') %>
<%= provide(:button_text, 'Create my company') %>
<h1>Create your company!</h1>

<div class="row">
    <div class="col-md-6 col-md-offset-3">
        <%= render 'form' %>
        <%= current_user.id %>
    </div>
</div>